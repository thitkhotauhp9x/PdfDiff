# How to run

```bash
docker compose up --build
docker compose run pdfdiff pdfdiff
```

# How to build
```bash
poetry build
docker compose up --build
docker compose run pdfdiff pdfdiff
```

```bash
poetry run pdfdiff
```